{
  "name": "servicenow-client-scoped-dts",
  "version": "0.1.0",
  "description": "JavaScript Typings for ServiceNow client-side scoped application development",
  "main": "server.js",
  "author": {
    "name": "Leonard T. Erwine"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "@types/node": "^8.10.59",
    "typescript": "^3.2.2"
  }
}
